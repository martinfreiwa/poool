---
methodology: CDDRP
methodology-version: 2.0
run-date: 2026-05-19
run-number: 1
scope: scan-dimensions B1-B8 + tooling C1/C2/C4/C5 + axis A15 (tests/bin)
prior-audits-deduped:
  - docs/security-audits/2026-04-28-security-auth-session.md
  - docs/security-audits/2026-05-16-admin-auth-followup.md
  - docs/security-audits/2026-05-16-wallet-frontend-production-readiness.md
  - docs/security-audits/2026-05-19-army-bug-audit.md
agents-launched: 13
phase: discovery-only (verification + remediation pending)
---

# CDDRP Run 1 — Discovery Report (Scan-Dimension Sweep)

This run targeted the **gaps** in `2026-05-19-army-bug-audit.md`, which was organised by domain band (Axis A) but did not run dedicated cross-cutting scans (Axis B) or external-tool sweeps (Axis C). All findings below are NEW vs the prior audit unless explicitly noted.

## §1 Aggregate metrics

| Metric | Value |
|---|---|
| Discovery agents launched | 13 (8 B + 4 C + 1 A15) |
| New Critical findings | ~18 |
| New High findings | ~44 |
| New Medium findings | ~70 |
| New Low findings | ~25 |
| Tooling: CVE | 0 (3 informational warnings) |
| Tooling: hardcoded secrets in source | 0 |
| Tooling: real-bug TODOs | 1 |
| Tooling: clippy correctness | 0 (1 suspicious in test) |

## §2 Critical findings (verified pending Phase 2)

### §2.1 Authorization (Axis B4)

| File:Line | CWE | Issue | Suggested permission |
|---|---|---|---|
| `backend/src/admin/marketplace.rs:2256` (`api_admin_marketplace_toggle_trading`) | 862 | Bare `AdminUser` on global trading kill-switch (Redis flag). Any admin role can halt or restart all secondary trading. | `marketplace.kill_switch` (new) or `marketplace.manage` |
| `backend/src/payments/routes.rs:1565` (`api_admin_approve_order`), `:1613` (`api_admin_reject_order`) | 862 | Inline `is_admin` only on bank-transfer order approval/rejection. Credits wallet + creates investment. | `payments.write` |
| `backend/src/admin/villa_operations.rs` 15 handlers (`:212, :304, :410, :419, :443, :664, :896, :1217, :1227, :1301, :1354, :1574, :1602, :1652, :1764`) | 862 | Bare `AdminUser` on the entire villa-operations admin surface: create/update/submit/approve/publish/reject/top-up/process-payouts/queue/link-doc/distribute/config-update/list. | `villa.operations.write` + `villa.operations.approve` (maker/checker split) |
| `backend/src/admin/villa_valuations.rs` 8 handlers (`:90, :151, :200, :255, :263, :286, :379, :421`) | 862 | Bare `AdminUser` on preview/create/update/submit/approve/publish/reject/list. Valuation drives NAV-per-token. | `villa.valuation.write` + `villa.valuation.approve` |
| `backend/src/admin/villa_capex.rs:37, :61, :144` | 862 | Bare `AdminUser` on CapEx list/approve/reject. Approval reduces distributable. | `villa.capex.approve` |
| `backend/src/admin/villa_developer_access.rs:32, :63, :107` | 863 | Bare `AdminUser` on grant/revoke of developer↔asset links. | `villa.developer_access.manage` |
| `backend/src/admin/villa_deduction_policy.rs:62, :84` | 862 | Bare `AdminUser` on policy list/create. Policy controls deductible expense categories. | `villa.deduction_policy.write` |
| `backend/src/admin/villa_forecast.rs:47, :69, :162, :185, :286` | 862 | Bare `AdminUser` on assumption list/upsert + suggestion list/accept/discard. | `villa.forecast.write` |
| `backend/src/admin/villa_nav_snapshot.rs:30` | 862 | Bare `AdminUser` triggers `run_snapshot_for_all_assets`. | `villa.snapshot.run` |
| `backend/src/developer/change_requests.rs` 8 handlers (`:309, :376, :429, :545, :626, :721, :787, :840`) | 862 | All `admin_list/detail/approve/reject/bulk_approve/bulk_reject/assign_self/unassign` use bare `is_admin`. `admin_approve` writes proposed values into `assets`. | `developer_projects.write` |

### §2.2 SSRF (Axis B3)

| File:Line | CWE | Issue | Evidence |
|---|---|---|---|
| `backend/src/rewards/service.rs:3266-3267` (validation) + `:3420-3429` (worker) + `:3322-3351` (exposure) | 918 | Affiliate webhook subscription. `create_webhook_subscription` validates only `starts_with("https://")` — no host parse, no IP block, no redirect cap. Worker `reqwest::Client::builder().timeout(15s)` with default `redirect::Policy::limited(10)` (vs `Policy::none()` used at `:1468` for legacy worker). HTTP status codes propagated to owner via `GET /api/affiliate/webhooks` → status-code oracle for internal-infra enumeration. | Active affiliate registers `https://attacker/302?to=http://169.254.169.254/computeMetadata/v1/instance/service-accounts/default/token` → server fetches GCP cloud metadata. In prod (per `reference_prod_infra.md`: WIF SA wired to Secret Manager + Cloud SQL), short-lived token leak feasible via HTTP-status fingerprinting. |

### §2.3 Test-time encryption-key compromise (Axis A15)

| File:Line | CWE | Issue |
|---|---|---|
| `backend/tests/checkout_wallet_http.rs:28, :31-32` + `auth_2fa_http.rs:26, :28-29` | 798 | `install_test_totp_key()` does `std::env::set_var("TOTP_SECRET_ENCRYPTION_KEY", "0123…ef")` unconditionally — no `POOOL_ENV` guard, no `DATABASE_URL`-hostname check. If `DATABASE_URL` ever points at prod when these tests run, `encrypt_totp_secret` re-encrypts real TOTP secrets under a public test key and writes them back to `user_settings`. |
| `backend/tests/developer_affiliate_team_http.rs:65-72` | 798 | Same pattern for `BANK_IBAN_ENCRYPTION_KEY = "0011…ff"`. IBAN PII / GDPR + payment-data risk if test runs against prod DB. |

### §2.4 XSS (Axis B2)

| File:Line | CWE | Issue |
|---|---|---|
| `frontend/platform/admin/community/posts.html:111-135` | 79 (Stored) | Admin posts-list builds rows with `${p.author_name}`, `${p.content}` (truncated), `${p.content_tags}`, `${p.user_id}` raw via `innerHTML`. No `escapeHtml`. Any community user can set a display name `<script>fetch('/api/admin/users/'+id+'/delete')</script>` and own every moderator. CSRF cookie is JS-readable → full ATO. |
| `frontend/platform/admin/community/post-detail.html:99-191` | 79 (Stored) | Post detail uses raw `p.content`, `p.content_tags`, `p.author_name`, `c.author_name`, `c.content`, `r.reporter_name`, `r.reason`, `reaction.author_name`, `reaction_type`. `editTags` onclick takes `tags.join(',')` into single-quoted JS string. |
| `frontend/platform/static/js/admin-asset-details.js:458` | 79 (Stored) | Concentration-risk banner inserts `topHolder.name` raw into `innerHTML`. Other inserts at `:462+` correctly use `esc(inv.name)` — only the banner was forgotten. |
| `backend/src/admin/pages.rs:428` | 79 (Reflected) | `Html(format!("<h1>Page not found</h1><p>Debug info: Tried file '{}', minijinja error: {}</p>", file, e))`. `file` derives from `req.uri().path()`; only `..` is blocked. |
| `frontend/platform/portfolio.html:1151` + `backend/src/portfolio/routes.rs:88`; also `checkout.html:205-207` + `payments/routes.rs:518/557/561` | 79 (Stored, JSON-in-script) | `serde_json::to_string` shipped via `<script type="application/json">{{ x_json \| safe }}</script>`. `serde_json` does **not** escape `</script>`. Asset title containing `</script><script>alert(document.cookie)</script>` breaks out. Today titles are admin-controlled (low likelihood), but the foot-gun ships on every page render. |

## §3 High findings (verified pending Phase 2)

### §3.1 SQL injection (Axis B1)

| File:Line | CWE | Issue |
|---|---|---|
| `backend/src/community/routes.rs:7396-7412` (`admin_get_community_audit_log`) | 89 | User-controlled `entity_type` and `action` query params interpolated raw into SQL after only `.replace('\'', "")`. Misses `--`, `/*`, `$tag$`, Unicode quotes. With `standard_conforming_strings=off`, `\'` bypass works. |
| `backend/src/community/routes.rs:7467-7483` (`admin_export_community_audit_log_csv`) | 89 | Exact copy of pattern above on the CSV-export sibling. |

### §3.2 Authorization (continued from §2.1)

11 additional High items: community admin_get_community_audit_log/CSV/ban-appeals/hide_post/lock_post (5), marketplace reconciliation/alerts ×6/watchlist ×6/rules ×6/views ×6, developer routes asset-ownership-only checks (2), DeveloperUser COALESCE drift (1), admin/legal still bare (1). Full list in §9 bundle catalogue.

### §3.3 Auth / session / CSRF (Axis B7)

| File:Line | CWE | Issue |
|---|---|---|
| `backend/src/settings/routes.rs:380-419` (`change_password_handler`) | 287 | Skips `require_step_up_2fa(..., FinancialAction::PasswordChange, ...)` despite the enum variant existing and documented "always require 2FA". Only current-password is verified. Stolen-cookie + phished-password = full account takeover. |
| `backend/src/rewards/routes.rs:1038-1145` (`api_affiliate_payout_request`) | 287 | Skips step-up despite enum variant `AffiliatePayoutRequest` documented "Always requires 2FA so a stolen session cookie can't trigger withdrawals". |
| `backend/src/settings/service.rs:841-906` (`change_email`) | 384 | No session invalidation (compare `change_password` which deletes all sessions), no step-up. Cookie-bearing attacker swaps email → triggers forgot-password to take over permanently. |
| `backend/src/auth/routes.rs:1507-1540` (OAuth link flow) | 287 | Binds Google account to current session with NO password challenge, NO step-up. Attacker attaches their own Google account → can re-sign-in as victim indefinitely. |
| `backend/src/auth/routes.rs:1562-1576` (OAuth callback session) | 384 | `create_session(..., remember_me=true, ip_address=None, user_agent=None)` — Google login always 30-day session, no IP/UA recorded. Also no `csrf::rotation_cookie()` (compare `login_submit:307`) → pre-auth CSRF token survives. |

### §3.4 File upload (Axis B8)

| File:Line | CWE | Issue |
|---|---|---|
| `backend/src/payments/routes.rs:766-815` (deposit `proof_of_transfer`) | 434/73 | (a) `field.bytes()` before size check; (b) no `sniff_mime` / `validate_*_mime`; (c) raw `field.file_name()` concatenated into `format!("proofs/{}/{}", user.id, name)` with no sanitisation. |
| `backend/src/rewards/routes.rs:1687-1703` (`api_affiliate_upload_tax_document`) | 434 | `field.bytes()` before size check; no MIME validation; hardcoded `"application/octet-stream"` to GCS. |
| `backend/src/support/service.rs:258-321` (`reply_to_ticket` attachment) | 434 | Skips `attachment_signature_matches()` that `submit_ticket` has (`:125`). MIME taken from client header. |

### §3.5 Resource leaks (Axis B6)

12 High findings — top entries:

| File:Line | CWE | Issue |
|---|---|---|
| `backend/src/marketplace/websocket.rs:48-51` | 770 | `CHANNELS` HashMap never evicts per-asset `broadcast::Sender` entries; 256-msg ring buffer each. Memory monotonically grows with asset count. |
| `backend/src/community/routes.rs:182-225` | 770/400 | `parse_and_notify_mentions` loops over EVERY `@mention` with no cap; single post with 10K mentions = 10K SELECT + 10K INSERT in fire-and-forget task. |
| `backend/src/community/routes.rs:229-263` | 770 | `parse_and_store_opengraph` calls `res.text().await` with no byte cap; only 3s timeout. Malicious URL returning multi-GB body fills RAM. |
| `backend/src/blockchain/event_indexer.rs:383, :402` | 770 | `Vec::with_capacity(ids_count)` where `ids_count` is parsed from on-chain event hex; no bound. Crafted event with `ids_count = 2^32` OOMs indexer. |

Plus 8 unbounded admin queries (users/withdrawals/kyc/circle-members/reports/treasury-investors/affiliate-referrals/villa-operations-list) — all return entire tables.

### §3.6 Money math (Axis B5)

| File:Line | CWE | Issue |
|---|---|---|
| `backend/src/marketplace/validation.rs:325, :365` | 190 | `new_total = current_owned + additional_tokens` (i32+i32) unchecked. Buy with `quantity ≈ i32::MAX` wraps `new_total` negative, bypasses 80% concentration cap. |
| `backend/src/marketplace/service.rs:875, :889` | 190 | Trade-history `total = price.saturating_mul(qty)`, then `net = total ± fee` unchecked. If saturating_mul saturates at i64::MAX, the `+ fee` wraps → corrupted tax-report net. |

### §3.7 Tests + bin (Axis A15)

| File:Line | CWE | Issue |
|---|---|---|
| `backend/tests/leaderboard_integration.rs:1181, :1300, :1339, :1378, :1428, :1545` | 89-adj/1188 | `format!("TRUNCATE TABLE {table}")` with hardcoded production table names. Only guard is `#[ignore]` → `cargo test -- --ignored` against wrong DATABASE_URL wipes leaderboard tables. |
| `backend/tests/storage_phase1_audit.rs:79, :101, :124` | 489 | `std::env::set_var("POOOL_ENV", "production")` inside `#[test]`. Rust 2021 `set_var` is not thread-safe; storage code reads `POOOL_ENV` on tokio worker threads. UB window. |
| `backend/src/bin/backfill_villa_operations.rs:33-35, :116-138` | 1188 | Production data-migration bin accepts any `DATABASE_URL` with `--execute`. No `POOOL_ENV` guard, no confirmation prompt. Legacy USD-vs-IDR cents mismatch only documented in a comment. |

## §4 Medium findings (condensed)

- **Money math (B5):** 10 items — `cart/routes.rs` repeats deferred patterns (`format_cart_usd` negative-sign drop, `format_idr` precision loss, unchecked cart-subtotal multiplications), `event_indexer.rs` u256→i64 truncation, `payments/service.rs:1324` Decimal→i64 saturate, `rewards/service.rs:1175` commission_rate_bps unchecked mul, `wallet/routes.rs:1387` cancel-refund saturating_add, `marketplace/settlement.rs:429, :447` order-fill wrap, `dividends/service.rs:249` accumulator wrap (compounds deferred `:245` bug).
- **Resource leaks (B6):** 20 admin/community/marketplace queries without LIMIT or with too-high LIMITs (10K), one HashMap without TTL (`MATCH_PREVIEW_LIMITER`), email-campaign loads entire user set + sequential INSERTs.
- **Auth (B7):** 5 items — non-constant-time OAuth `state_cookie` compare, `verify_email_page` is GET with side effects (link-prefetcher consumes token), admin role grant/revoke no session invalidation + no step-up, post-2FA cookie loses `max_age`, `unlink_oauth_account` no password challenge.
- **Upload (B8):** 3 items — villa-doc filename CRLF-only sanitisation, `read_multipart_file` not migrated to chunked helper (3 callers), admin asset upload uses `field.bytes()`.
- **XSS (B2):** 6 items — multiple `Html(format!("...{}...", e))` 500-error pages (same fix-bundle as `routes_helper.rs`), CSS-injection via `style="background-image: url('{{ x }}')"`, latent escapeHtml-variant drift.
- **Authz (B4):** 4 items — `marketplace.rs` compliance helpers bypass AdminUser is_active check, `admin/dashboard.rs:20` no permission gate, prior-flagged storage handlers still bare on disk.
- **Tests + bin (A15):** 4 items — 11 HTTP tests unconditionally set POOOL_ENV=development, marketplace concurrent-load test leaks fixture rows on panic, community DB fallback.

## §5 Low findings (condensed)

~25 items including: clippy `await_holding_lock` in test, ~6 style lints, 5 SSRF defense-in-depth (auth/KYC/IPFS/Slack: missing reqwest timeouts), web-push endpoint no host allowlist, JS float-cents in admin filters, JSDoc/format-string placeholders mis-flagged as TODO, `target="_blank"`-without-rel residual sites, archived `_archive/` references.

## §6 False-positive registry (Phase 1 self-checks)

| Finding | Reason FP |
|---|---|
| 3 keccak256 event-topic hashes (B4 secret grep) | Public on-chain identifiers, not secrets |
| pgbouncer `${VAR}` template (C4) | Shell-variable interpolation, not literal |
| CI `postgres://postgres:postgres@localhost` (C4) | CI-local container, not a credential |
| Multiple SQL audit-payload test strings (`'; DROP TABLE`) | Bound as `$1` parameter, not interpolated |

## §7 NEEDS_REPRO registry

| Finding | PoC needed |
|---|---|
| B6 — `event_indexer.rs:383, :402` Vec::with_capacity bomb | Craft an on-chain `TransferBatch` event with `ids_count = 2^32`; observe whether `Vec::with_capacity` aborts before parsing the array body. |
| B5 — concentration-cap bypass | Submit buy order with `quantity = i32::MAX` to an asset where `current_owned >= 1`; observe whether `new_total` wraps before the validation check. |
| B3 — SSRF status-code oracle to GCP metadata | Register affiliate webhook to attacker-controlled host that 302s to `169.254.169.254`; observe `last_status_code` in `/api/affiliate/webhooks`. |

## §8 DOWNGRADED registry

None applicable until Phase 2 verification.

## §9 Bundle catalogue

| ID | Pattern | Count | Fix shape | Complexity |
|---|---|---|---|---|
| F1+ | Bare `AdminUser` / inline `is_admin` without `require_permission` | **~60 handlers** (10 Critical + 11 High + 4 Med + ~35 marketplace compliance/alerts/views) | Add `admin.require_permission(&state.db, "<slug>").await?;` per handler | Moderate |
| F9 (new) | `tokio::spawn` fire-and-forget tasks with no per-spawn timeout / no concurrency bound | dozens across admin/, community/, marketplace/ | Wrap in `JoinSet` with cap + `tokio::time::timeout` | Moderate |
| F10 (new) | `Html(format!("<h1>...: {}</h1>", e))` reflecting MiniJinja / sqlx error | 8+ sites (admin/pages.rs:419/428, rewards/routes.rs:2538, blog/routes.rs:181/276, developer/routes.rs:276/314, assets/routes.rs:653/870, portfolio/routes.rs:101) | Static body + `tracing::error!` | Trivial |
| F11 (new) | `serde_json::to_string` shipped via `\| safe` inside `<script>` without `</script>` escape | 4 sites (portfolio.html, checkout.html, ×3 JSON blobs) | Helper `to_safe_json_script(v)` replacing `</` → `<\/` + ` `/` ` | Trivial |
| F12 (new) | `format!()` building SQL with WHERE/IN-list construction (latent — currently safe) | 5+ sites | Convert to `ANY($N)` binds or compile-time literal | Moderate |
| F13 (new) | `field.bytes()` before size check in upload handlers (NEW sites not in prior Bundle 2) | 3 sites (`payments/routes.rs:766`, `rewards/routes.rs:1687`, `admin/assets.rs:976/1096`) | Migrate to `read_field_capped` (helper at `backend/src/storage/upload_helpers.rs`) | Trivial |
| F14 (new) | `target="_blank"` without `rel="noopener noreferrer"` residual | ~13 sites (landing-v2, landing, property-public, marketplace-trading-v3, admin/asset-details, admin/blockchain-treasury, admin/settings, admin-blockchain-treasury.js) | Append `rel="noopener noreferrer"` | Trivial — *partially fixed in prior run* |
| F15 (new) | `tokio::spawn` long-running tasks holding `std::sync::MutexGuard` across `.await` | 1 site (test) + audit risk for prod copies | Switch to `tokio::sync::Mutex` or release guard before await | Trivial |
| F16 (new) | Critical-flow handlers missing `require_step_up_2fa` | 4 sites (`change_password`, `change_email`, `affiliate_payout_request`, OAuth link) | Add `require_step_up_2fa(...).await?;` matching the enum-defined `FinancialAction` | Trivial-Moderate |

## §10 Specialist flow audits (Phase 2C)

Deferred to Phase 2.

## §11 Remediation log (Phase 3)

| Finding | File:Line | Fix landed | Build |
|---|---|---|---|
| B3 SSRF — affiliate webhook URL validation | `backend/src/rewards/service.rs:3266-3270` | Added `validate_postback_url(url).await?` call after the HTTPS prefix check. | ✓ |
| B3 SSRF — subscription worker redirect policy | `backend/src/rewards/service.rs:3420-3425` | Added `.redirect(reqwest::redirect::Policy::none())` to the `reqwest::Client::builder()` chain (matches legacy worker at `:1466-1469`). | ✓ |
| B2 — admin-asset-details concentration banner | `frontend/platform/static/js/admin-asset-details.js:458` | Wrapped `topHolder.name` interpolation in `esc(...)` to match the sibling at `:475`. | n/a |
| F10 — admin/pages.rs:419 render-error leak | `backend/src/admin/pages.rs:419` | Static body + `tracing::error!`. | ✓ |
| F10 — admin/pages.rs:428 404 reflected (B2 path) | `backend/src/admin/pages.rs:428` | Static `<h1>Page not found</h1>`, `file`+`e` logged via existing `tracing::error!`. | ✓ |
| F10 — rewards invoice render leak | `backend/src/rewards/routes.rs:2538` | Static body + new `tracing::error!`. | ✓ |
| F10 — blog render leak (1) | `backend/src/blog/routes.rs:181` | Static body. | ✓ |
| F10 — blog render leak (2) | `backend/src/blog/routes.rs:276` | Static body. | ✓ |
| F10 — assets render leak (1) | `backend/src/assets/routes.rs:653` | Static body. | ✓ |
| F10 — assets render leak (2) | `backend/src/assets/routes.rs:870` | Static body. | ✓ |
| F10 — portfolio render leak | `backend/src/portfolio/routes.rs:101` | Static body + new `tracing::error!` on both render-err and template-not-found paths. | ✓ |
| F13 — payments deposit-proof chunked upload | `backend/src/payments/routes.rs:766` | `read_field_capped(&mut field, MAX_PROOF_BYTES, "proof")`; new 10MB constant introduced because no cap existed. | ✓ |
| F13 — rewards tax-doc chunked upload | `backend/src/rewards/routes.rs:1690` | `read_field_capped(..., 10MB, "tax_document")`; `ApiError → AppError` wrapper matching existing template at `:1948`. | ✓ |
| F13 — admin asset image chunked upload | `backend/src/admin/assets.rs:976` | `read_field_capped(..., MAX_ADMIN_ASSET_IMAGE_BYTES, "image")`. | ✓ |
| F13 — admin asset document chunked upload | `backend/src/admin/assets.rs:1096` | `read_field_capped(..., MAX_ADMIN_ASSET_DOCUMENT_BYTES, "document")`. | ✓ |

**Total: 14 sites remediated across 4 parallel agents. Final `cargo check`: clean (against live DB).**

### Phase 3.2 — continuation (5 parallel agents)

| Finding | File:Line | Fix landed | Build |
|---|---|---|---|
| Community OG SSRF — Critical (carried-over from prior audit) | `backend/src/community/routes.rs` `parse_and_store_opengraph` | URL parse + scheme allowlist + host allowlist + IP block (literal + DNS-resolved) + `redirect::Policy::none()`. Inline `is_blocked_og_ip` mirrors `rewards/service.rs::is_blocked_postback_ip`. | ✓ |
| OG body unbounded — High (B6) | same function | Chunked `res.chunk()` accumulation with `MAX_OG_BYTES = 1 MB` cap; bails on overflow. | ✓ |
| Mention amplification — High (B6) | `parse_and_notify_mentions` | `MAX_MENTIONS_PER_POST = 50` cap with single `tracing::warn!` on over-cap (audit signal). | ✓ |
| F11 JSON-in-script helper | `backend/src/common/json_safe.rs` (new) + `mod.rs` | Helper `to_safe_json_script` escapes `</` → `<\/`, U+2028/9 → `\u202[89]`. | ✓ |
| F11 portfolio.html JSON island | `backend/src/portfolio/routes.rs:88` | Replaced `serde_json::to_string` with `crate::common::json_safe::to_safe_json_script`. | ✓ |
| F11 checkout.html cart_json | `backend/src/payments/routes.rs:518` | Same. | ✓ |
| F11 checkout.html wallet_json | `backend/src/payments/routes.rs:557` | Same. | ✓ |
| F11 checkout.html bank_json | `backend/src/payments/routes.rs:560` | Same. | ✓ |
| B5 concentration cap i32 wrap (Site 1) | `backend/src/marketplace/validation.rs:325` (`check_concentration_limit_tx`) | `checked_add` with `AppError::BadRequest` on overflow. | ✓ |
| B5 concentration cap i32 wrap (Site 2) | `backend/src/marketplace/validation.rs:365` (`check_concentration_limit`) | `checked_add` with `OrderRejection::InvalidQuantity` (matches existing error idiom). | ✓ |
| B4 dev change_requests admin (8 handlers) | `backend/src/developer/change_requests.rs:309, :376, :429, :545, :626, :721, :787, :840` | Migrated to `AdminUser` extractor + `require_permission("developer_projects.view")` for reads, `.write` for mutations. | ✓ |
| A15 test prod-host guard (3 files) | `backend/tests/checkout_wallet_http.rs`, `auth_2fa_http.rs`, `developer_affiliate_team_http.rs` | `mod safety { fn assert_database_url_is_local() }` panic-on-non-local-DSN; wired into `pool()` + `install_test_totp_key()` + `make_state()`. | ✓ (`--tests` mode clean) |

**Phase 3.2 total: 19 additional sites remediated (5 parallel agents). Combined Phase 3 + 3.2: 33 sites. Final `cargo check --tests`: clean.**

### Phase 3.3 + 3.4 — large mechanical sweeps (7 parallel agents)

| Finding | File:Line | Fix landed | Build |
|---|---|---|---|
| **B4 marketplace admin RBAC** (26 handlers) | `backend/src/admin/marketplace.rs` (various) | Per-handler `admin.require_permission(&state.db, "<slug>")` with: `marketplace.manage` (kill-switch), `marketplace.compliance` (alerts/watchlist/rules — 18 handlers), `marketplace.view` (views, push subs, alert_history, reconciliation — 7 handlers). | ✓ |
| **B4 villa subsystem RBAC** (38 handlers / 7 files) | `villa_operations.rs` (15), `villa_valuations.rs` (8), `villa_capex.rs` (3), `villa_developer_access.rs` (3), `villa_deduction_policy.rs` (2), `villa_forecast.rs` (5), `villa_nav_snapshot.rs` (1) + 1 extra ops handler | Slug pattern `villa.<area>.{view,write,approve,manage}` per audit §13 decision. Maker/checker split: `publish` + `process_payouts` + valuation `publish/reject` + capex `approve/reject` all on `.approve`. | ✓ |
| **B6 admin unbounded queries (4 sites)** | `admin/users.rs:375`, `admin/withdrawals.rs:67`, `admin/kyc.rs:42`, `rewards/routes.rs:1622` | `LIMIT 500` + `?page=` offset pagination. | ✓ |
| **B6 treasury dividends/calculate** | `admin/treasury.rs:196` | Skipped (correctness requires every active investor). Added `tracing::warn!` when count > 10K + comment for future paginated rework. | ✓ |
| **B6 community unbounded queries (2 sites)** | `community/circles.rs:396` (`get_circle_members`), `community/service.rs:753` (`get_pending_reports`) | `LIMIT 100` and `LIMIT 200` respectively. Pagination plumb deferred (5 internal call sites). | ✓ |
| **B7 change_email session invalidation** | `settings/service.rs` (after :888) | `DELETE FROM user_sessions WHERE user_id = $1` after the email UPDATE, matching the existing `change_password` pattern. | ✓ |
| **B8 deposit-proof filename sanitisation** | `payments/routes.rs:793` | ASCII-alphanumeric + `.-_` filter, leading-dot strip, 64-char cap, fallback `"proof"`, UUID prefix to prevent overwrite of prior proofs. | ✓ |
| **B6 event_indexer Vec::with_capacity bomb** | `blockchain/event_indexer.rs` (both `ids_count` and `values_count` sites) | `MAX_TRANSFER_BATCH_IDS = 10_000` cap with `tracing::warn!` on over-cap + early `return None`. | ✓ |
| **B5 trade-history net wrap** | `marketplace/service.rs:875` (total) and `:889` (net) | `checked_mul` with row-skip on overflow at :875; `checked_add`/`checked_sub` with fallback-to-`total` + log at :889. | ✓ |
| **B7 admin role grant/revoke session invalidation** | `admin/settings.rs:217, :252, :312` | `DELETE FROM user_sessions WHERE user_id = $1` after each role mutation (add_admin / remove_admin / update_admin_role). 5 sibling handlers in `admin/access.rs` correctly skipped (no role mutation). | ✓ |

**Phase 3.3 + 3.4 total: 66 additional sites remediated (7 parallel agents).**

### Phase 3.5 — high-value blockers (3 parallel agents)

| Finding | File:Line | Fix landed | Build |
|---|---|---|---|
| **B2 admin community stored XSS (Critical)** | `frontend/platform/admin/community/posts.html`, `post-detail.html` | Added `escapeHtml` / `escapeAttr` / `escapeJsString` helpers + wrapped 27 API-sourced interpolations across both files. Double-nested `editTags` onclick fixed via `escapeAttr(escapeJsString(...))`. 23 interpolations left intentionally unwrapped (UUIDs, numerics, locally-constructed HTML literals) with classification. | n/a (HTML) |
| **B6 WS broadcast channel-map leak (High)** | `backend/src/marketplace/websocket.rs` | Option-1 subscriber-count eviction at disconnect (TOCTOU-safe with double-check under write-lock). `MAX_CHANNELS = 100_000` hard cap with `tracing::warn!`. No external wiring needed. | ✓ |
| **B7 `verify_email_page` GET → POST (Medium)** | `backend/src/auth/routes.rs:170`, new `verify_email_confirm_handler`, `backend/src/auth/service.rs::verify_email` | GET renders minimal HTML form with hidden token + auto-submit JS + `<noscript>` fallback button. POST `/auth/verify-email/confirm` performs state mutation. Sibling tokens for same user also invalidated (defense-in-depth, mirrors `reset_password` pattern at service.rs:886). | ✓ |

**Phase 3.5 total: 30+ additional sites/changes across 3 parallel agents.**

### Phase 3.6 — final mechanical sweeps + quality review (3 parallel agents)

| Finding | File:Line | Fix landed | Build |
|---|---|---|---|
| **B6 email-campaign mass-insert** | `backend/src/admin/emails.rs` `api_admin_emails_send_campaign` | Audience capped at `LIMIT 100_000` with `tracing::warn!`. Per-user loop replaced with batched `INSERT … VALUES (), ()` via `sqlx::QueryBuilder::push_values` in 500-row chunks. Per-chunk transactions; partial-progress preferred over total loss on chunk failure. | ✓ |
| **B7 `api_roles_update_permissions` session propagation** | `backend/src/admin/access.rs` | Collects affected role IDs across the bulk loop, single post-commit `DELETE FROM user_sessions WHERE user_id IN (SELECT user_id FROM user_roles WHERE role_id = ANY($1) AND is_active = TRUE)` with logging of rows-affected. Sibling handlers correctly skipped. | ✓ |
| **Quality-review pass (read-only)** | All session changes (~78 files / +1,696 / -690 LOC) | Verdict: **READY-TO-COMMIT** with caveats. See §14. | n/a |

**Phase 3.6 total: 2 fixes + 1 quality-review verdict (3 parallel agents).**

### Run totals (final)

| Phase | Agents | Sites |
|---|---|---|
| Phase 1 (discovery) | 13 | n/a (157 findings) |
| Phase 2 (verification) | 5 | n/a (verdicts on the audit's top Crit+High) |
| Phase 3 (initial remediation) | 4 | 14 |
| Phase 3.2 (continuation) | 5 | 19 |
| Phase 3.3 + 3.4 (mechanical sweeps) | 7 | 66 |
| Phase 3.5 (high-value blockers) | 3 | 30+ |
| Phase 3.6 (final sweeps + QA) | 3 | 2 fixes + 1 QA |
| **Run 1 total** | **40 agents** | **131+ sites remediated** |

`cargo check --tests` against live DB: **clean** after every phase.

## §14 Quality-review caveats (Phase 3.6)

Diff scope: 78 files touched (`+1,696 / -690` lines, net `+1,006`). No High-severity regression risks detected. All defensive edits and module declarations validated.

**Three caveats before commit:**

1. **Untracked files predating this session must be `git add`-ed.** `backend/src/admin/developer_applications.rs` and `database/203_developer_applications.sql` (+ rollback) appear as `??` in the original `git status` — they are user WIP, not session output. The session-fixed files reference them via `admin/mod.rs`, but the user must stage them before they compile-and-run on a fresh checkout.

2. ~~New permission slugs require a follow-up DB seed migration.~~ **CLOSED in Phase 3.7** — `database/204_cddrp_run1_permission_slugs.sql` (+ rollback) seeds all 24 new slugs with sensible defaults: super_admin gets everything, admin inherits via `'all'`, compliance/finance/support get curated subsets (e.g. compliance reads villa + valuation-approve for independent oversight; finance reads villa + approves operations/valuations/capex). Migration roundtrip-tested on the live local DB (45 inserts → 0 after rollback → 45 after re-apply). Pre-existing slug gap (`approvals.manage`, `marketplace.edit`, `platform.manage`, `users.edit`) also seeded to super_admin.

3. **Pre-existing developer/villa_operations.rs N+1 query** observed in `api_developer_villa_operations_list` (sequential `load_other_notes` per row, no LIMIT). Introduced by user's WIP, not by this session. Easy fix: fold `expense_other_notes` into the main `SELECT *` query. Worth doing before merge but out-of-scope for this run (file is in WIP set).

Test impact: zero session-fix-induced test regressions detected. New `admin/settings.rs` session-invalidation handlers + `admin/access.rs::api_roles_update_permissions` session propagation are not covered by existing tests — recommend adding integration coverage in a follow-up.

## §12 Deferred-finding registry

| Finding | Severity | Blocker (§4.6.1 precondition that failed) |
|---|---|---|
| B4 villa-subsystem RBAC (26+ handlers) | Critical | §4.6.1#3 — needs design conversation: choice of permission slug per handler, maker/checker split for approve paths, migration of existing admin roles. Too many handlers for a single mechanical sweep. |
| B4 marketplace kill-switch (`api_admin_marketplace_toggle_trading`) | Critical | §4.6.1#3 — needs new permission slug (`marketplace.kill_switch`) decision. |
| B4 payments approve/reject order | Critical | §4.6.1#3 — needs slug decision (`payments.write` vs new `orders.approve`). |
| B4 developer change_requests admin (8 handlers) | Critical | §4.6.1#3 — slug `developer_projects.write` already exists; sweep would be moderate, deferred for batch with other RBAC bundles. |
| B4 community admin: hide_post / lock_post / audit_log / ban_appeals (5 handlers) | High | §4.6.1#3 — needs `community.audit.read`, `community.manage` slug decisions. |
| B4 marketplace admin: alerts / watchlist / rules / views (~25 handlers) | High | §4.6.1#3 — slug `marketplace.compliance` exists; large mechanical sweep, deferred for batch. |
| F16 step-up 2FA missing on critical flows (4 sites) | High | §4.6.1#5 — needs business decision on friction (every password change, every email change, every payout, every OAuth link → all require 2FA?). |
| 2FA login-time bypass | Critical | §4.6.1#5 — comment says "temporarily disabled"; user must authorise restoration. |
| Settlement private key as `cast --private-key` CLI arg | Critical | §4.6.1#3 — refactor needs `cast` semantic verification. |
| Dividend per-holder math (`backend/src/dividends/service.rs:245`) | Critical | §4.6.1#3 — needs redesign + regression test. |
| `/uploads` directory served unauthenticated | Critical | §4.6.1#3 — needs auth-gated static-file design. |
| DM rate-limit + privacy-flag bypass | Critical | §4.6.1#3 — needs per-pair vs per-recipient rate-limit design. |
| SSRF in OpenGraph parser | Critical | §4.6.1#3 — needs private-IP allowlist design (now partially mirrored by the affiliate-webhook fix). |
| WS broadcast channel map memory leak | High | §4.6.1#3 — needs TTL / disconnect-driven eviction design. |
| Mention amplification (10K @-mentions → 10K queries) | High | §4.6.1#3 — needs per-post mention cap + batch query design. |
| OG parser unbounded body | High | §4.6.1#3 — needs `take(N)` byte cap design. |
| Concentration-cap i32 wrap | High | §4.6.1#3 — fix is `checked_add` but compliance reviewer should confirm intended semantics. |
| 8 admin unbounded queries | High | §4.6.1#4 — multi-file mechanical sweep, scoped for next run. |
| B2 admin community posts.html + post-detail.html stored XSS | Critical | §4.6.1#3 — large multi-section HTML rewrite; needs scoped review (escapeHtml import or rewrite via `textContent`). |
| B2 JSON-in-script (`portfolio.html`, `checkout.html`) | High | §4.6.1#3 — needs `to_safe_json_script` helper + 4 callsite migration; bundled. |
| F11 hardcoded operator emails (~25 refs / 13 files) | Medium | §4.6.1#3 — needs `platform_settings` schema + value-load wiring. |
| A15 test-side TOTP/IBAN keys | Medium (downgraded) | §4.6.1#3 — needs prod-host guard helper in test harness. |
| B7 change_password / change_email no session invalidation | High | §4.6.1#3 — invalidation semantics need product call. |
| OAuth link no password challenge | High | §4.6.1#3 — needs UX flow design. |

## §13 Next-action recommendations (updated after Phase 3.2)

Already-landed across Phase 3 / 3.2 / 3.3 / 3.4 (struck through):

- ~~F11 JSON-in-script helper + 4 callsites~~ ✓
- ~~Community OG SSRF + body cap + mention amplification~~ ✓
- ~~B5 concentration cap i32 wrap~~ ✓
- ~~B4 dev change_requests admin RBAC (8 handlers)~~ ✓
- ~~A15 test prod-host guard~~ ✓
- ~~B4 villa subsystem RBAC (38 handlers / 7 files)~~ ✓
- ~~B4 marketplace admin RBAC (26 handlers)~~ ✓
- ~~B6 admin unbounded queries (4 sites paginated + 2 community sites bounded)~~ ✓
- ~~B7 change_email session invalidation~~ ✓
- ~~B7 admin role grant/revoke session invalidation (3 sites in admin/settings.rs)~~ ✓
- ~~B8 deposit-proof filename sanitisation + UUID-prefix~~ ✓
- ~~B6 event_indexer Vec::with_capacity bomb (both sites)~~ ✓
- ~~B5 trade-history net wrap (both sites)~~ ✓
- ~~B4 marketplace kill-switch + order approve/reject~~ ✓ (rolled into marketplace sweep)

Remaining true deferrals — need user input or large-surface work:

1. ~~B2 admin community posts.html + post-detail.html stored XSS~~ ✓ (Phase 3.5)
2. **F16 step-up 2FA restoration** for `change_password` / `payout_request` / OAuth link. Blocked on UX/friction product decision.
3. **2FA login-time bypass** (`auth/routes.rs:253`, `:1556`). Hardcoded `is_2fa_verified=true`; comment says "temporarily disabled" — user must authorise restoration.
4. **Settlement private key as `cast --private-key` CLI arg**. Refactor needs `cast` semantic verification.
5. **Dividends per-holder math** (`dividends/service.rs:245`). Needs redesign + regression test.
6. **`/uploads` directory served unauthenticated** (`lib.rs:1722`). Needs auth-gated static-file design.
7. **DM rate-limit + privacy-flag bypass**. Per-pair vs per-recipient design call.

Lower-priority remaining mechanical bundles:
- ~~WS channel-map eviction policy~~ ✓ (Phase 3.5)
- Email-campaign mass-insert batching (`admin/emails.rs`)
- ~~B7 `verify_email_page` GET→POST conversion~~ ✓ (Phase 3.5)
- `api_roles_update_permissions` session invalidation propagation (broader pattern: invalidate all sessions holding any affected role)
- F1 hardcoded operator emails → `platform_settings` migration (~25 references / 13 files)
- Various Medium tier items from §4
